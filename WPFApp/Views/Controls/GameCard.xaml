<UserControl x:Class="WPFApp.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPFApp"
             xmlns:utils="clr-namespace:Utils"
             Width="440" Height="220">
    <UserControl.Resources>
        <utils:CentsToCurrencyConverter x:Key="CentsToCurrencyConverter"/>
    </UserControl.Resources>

    <Border CornerRadius="10"
            BorderThickness="1"
            BorderBrush="#DDDDDD"
            Background="#F8F8F8"
            SnapsToDevicePixels="True"
            Padding="0"
            Margin="5">
        <Grid>
            <!-- Imagen a pantalla completa -->
            <Image Source="{Binding UrlImagen}"
                   Stretch="UniformToFill"
                   Opacity="0.95"
                   ClipToBounds="True"/>

            <!-- Franja inferior con el precio -->
            <Border Background="#CC000000"
                    Height="30"
                    VerticalAlignment="Bottom">
                <TextBlock Text="{Binding Amount, Converter={StaticResource CentsToCurrencyConverter}}"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontWeight="Bold"
                           FontSize="14"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
