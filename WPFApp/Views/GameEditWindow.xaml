<Window x:Class="WPFApp.GameEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="700" Width="500"
        Style="{StaticResource CustomWindowStyle}"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding WindowTitle}"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextMainBrush}"
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel>

                <TextBlock Text="TÍTULO" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Text="{Binding EditableGame.Title, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15"/>

                <TextBlock Text="PUBLISHER" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Text="{Binding EditableGame.Publisher}" Height="35" Margin="0,0,0,15"/>

                <TextBlock Text="DEVELOPER" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Text="{Binding EditableGame.Developer}" Height="35" Margin="0,0,0,15"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="CATEGORÍA" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding EditableGame.Category}" Height="35"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="PRECIO" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding EditableGame.Amount}" Height="35"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="URL IMAGEN" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Text="{Binding EditableGame.UrlImagen}" Height="35" Margin="0,0,0,15"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="FECHA LANZAMIENTO" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding EditableGame.ReleaseDate}" Height="35" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="ACTIVO" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <CheckBox IsChecked="{Binding EditableGame.IsActive}" VerticalAlignment="Center">
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                            </CheckBox.LayoutTransform>
                        </CheckBox>
                    </StackPanel>
                </Grid>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#FF5555"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Margin="0,5,0,10"/>

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            
                            Content="ELIMINAR"
                            Command="{Binding DeleteCommand}"
                            Background="#D32F2F" 
                            Foreground="White"
                            FontWeight="Bold"
                            Height="40"
                            Padding="15,4"
                            Cursor="Hand"
                            BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#B71C1C"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditMode}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="CANCELAR"
                                Height="40"
                                Width="100"
                                Margin="0,0,10,0"
                                Background="Transparent"
                                Foreground="{StaticResource TextMutedBrush}"
                                BorderBrush="{StaticResource TextMutedBrush}"
                                BorderThickness="1"
                                Cursor="Hand"
                                Command="{Binding CancelCommand}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="1" 
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="White"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="GUARDAR"
                                Height="40"
                                Width="100"
                                Style="{StaticResource PrimaryButton}"
                                Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>